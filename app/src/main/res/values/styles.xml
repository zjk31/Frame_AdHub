<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 日间启动页：pre-API 31 用 windowBackground，API 31+ 用 windowSplashScreenBackground -->
    <style name="LaunchTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- MainActivity 主题：仅设 windowBackground 消除白屏，不触发 SplashScreen API -->
    <style name="MainTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">#FFF5F5F5</item>
    </style>
</resources>
